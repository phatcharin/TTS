<?xml version="1.0" encoding="utf-8"?>
<!--
  ~ Copyright (c) 2015-present, Parse, LLC.
  ~ All rights reserved.
  ~
  ~ This source code is licensed under the BSD-style license found in the
  ~ LICENSE file in the root directory of this source tree. An additional grant
  ~ of patent rights can be found in the PATENTS file in the same directory.
  -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.parse.starter" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".Login.StarterApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <meta-data
            android:name="com.parse.APPLICATION_ID"
            android:value="@string/parse_app_id" />
        <meta-data
            android:name="com.parse.CLIENT_KEY"
            android:value="@string/parse_client_key" />

        <activity
            android:name=".Login.Login.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login.Login.LoginSignupActivity" >
        </activity>
        <activity android:name=".Login.Login.Welcome" >
        </activity>
        <activity android:name=".Login.Login.SignupActivity" >
        </activity>

        <activity
            android:name=".Login.Flashcard.CategoriesActivity"
            android:label="@string/app_name" >
        </activity>

        <activity android:name=".Login.Flashcard.ShowActivity"
            android:label="@string/app_name" >
        </activity>
        <activity android:name=".Login.Flashcard.AddWordActivity"
            android:label="@string/app_name" >
        </activity>
        <activity android:name=".Login.Flashcard.ChooseGames"
            android:label="@string/app_name" >
        </activity>
        <activity android:name=".Login.Flashcard.GameMatching"
            android:label="@string/app_name" >
        </activity>
        <activity android:name=".Login.Flashcard.GameTrueOrFalse"
            android:label="@string/app_name" >
        </activity>
        <activity android:name=".Login.Flashcard.ShowScore"
            android:label="@string/app_name" >
        </activity>
        <activity android:name=".Login.Flashcard.GameAnswer"
            android:label="@string/app_name" >
        </activity>

    </application>

</manifest>
